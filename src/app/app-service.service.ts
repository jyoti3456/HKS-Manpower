import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class AppServiceService {
  // startWatching: BehaviorSubject<number> = new BehaviorSubject<number>(0);
  constructor() { 

  }


  
}
